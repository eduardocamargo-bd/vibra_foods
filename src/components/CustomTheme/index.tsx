'use client'

import React from 'react'
import '../../app/vibra-theme.css'

const CustomTheme: React.FC = () => {
  // Este componente apenas importa o arquivo vibra-theme.css 
  // que contém todas as variáveis CSS do design system Vibra Foods

  return null
}

export default CustomTheme